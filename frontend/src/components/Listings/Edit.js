import React, { Component } from 'react';
import { connect } from 'react-redux';

//material ui
import { Container, Typography, withStyles } from '@material-ui/core';

const Edit = () => {
  return <Typography>Edit Listing</Typography>;
};

export default Edit;
